{
  "name": "cantika-pos-enterprise",
  "version": "1.0.0",
  "description": "Cantika Beauty Store Enterprise POS & Inventory Management System (Frontend & Backend)",
  "scripts": {
    "start": "node backend/server.js",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npx ng serve --port 4200",
    "build:frontend": "cd frontend && npx ng build --configuration production",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  }
}
